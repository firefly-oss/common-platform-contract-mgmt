package com.firefly.core.contracts.interfaces.enums;

/**
 * Term data type enumeration matching the database enum term_data_type_enum
 */
public enum TermDataTypeEnum {
    STRING,
    NUMBER,
    DECIMAL,
    BOOLEAN,
    DATE,
    DATETIME,
    ENUM,
    JSON,
    MONEY
}
