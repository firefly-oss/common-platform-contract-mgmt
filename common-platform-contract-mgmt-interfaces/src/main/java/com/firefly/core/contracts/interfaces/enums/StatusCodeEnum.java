package com.firefly.core.contracts.interfaces.enums;

/**
 * Status code enumeration matching the database enum status_code_enum
 */
public enum StatusCodeEnum {
    CREATED,
    SUBMITTED_FOR_APPROVAL,
    APPROVED,
    REJECTED,
    ACTIVATED,
    SUSPENDED,
    TERMINATED,
    EXPIRED,
    CANCELLED,
    AMENDED
}
